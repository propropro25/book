(()=>{const p=window.SOURCE_PAGE;if(!p)return;const app=document.getElementById('sources-app');const sources=p.sources||[];app.innerHTML=`<header class="site-header"><a class="brand" href="https://propropro25.github.io/book/">A Theory of Integration</a><nav class="top-nav"><a href="https://propropro25.github.io/${p.chapterRepo}/">Chapter</a><a href="https://propropro25.github.io/TOC/">Contents</a></nav></header><section class="sources-hero"><div class="eyebrow">Chapter ${p.number} · Evidence</div><h1>${p.title}</h1><p>Original records, papers, images and first-person accounts connected to this chapter. Each source should state what it supports and what it cannot establish alone.</p></section><main class="source-library"><div class="source-toolbar"><h2>Source library</h2><span class="source-count">${sources.length} sources</span></div><div class="source-grid">${sources.length?sources.map(s=>`<a class="source-card" href="${s.url}" target="_blank" rel="noopener"><span class="source-kind">${s.kind}</span><h3>${s.title}</h3><p>${s.note}</p></a>`).join(''):`<div class="empty-source">No sources have been added yet. We’ll add each record here as we work through the chapter, along with the exact passage it supports, challenges or helps place in context.</div>`}</div></main>`;})();
